# ============================================================
#  VeriFYD — requirements.txt
# ============================================================

# Web framework
fastapi>=0.100.0
uvicorn>=0.23.0

# Required for Form() and UploadFile in FastAPI
python-multipart>=0.0.6

# Computer vision
opencv-python-headless==4.13.0.92
numpy>=2.0.0
Pillow>=10.0.0

# HTTP client for direct .mp4 URLs
requests>=2.31.0

# Video downloader
yt-dlp>=2025.1.1

# WebSocket client — used by SMVD RapidAPI
websocket-client>=1.6.0

# OpenAI GPT-4o vision analysis
openai>=1.0.0

# Resend email SDK for OTP verification
resend>=2.0.0

# Async job queue (Redis + RQ)
redis>=5.0.0
rq>=1.16.0

# PostgreSQL
psycopg2-binary>=2.9.0

# rPPG biological signal engine (Engine 5)
scipy>=1.11.0
mediapipe>=0.10.0

# Audio analysis engine (Engine 4)
librosa>=0.10.0
soundfile>=0.12.0

# DINOv2 ViT-Small feature-based detection (Engine 6)
# 21.7M params, ~350MB RAM — loads once at worker startup
# Used as tie-breaker for ambiguous signal scores (35-72 range)
torch>=2.0.0
torchvision>=0.15.0
transformers>=4.40.0

# C2PA content provenance verification (Engine 5)
# Reads cryptographic content credentials from media files
# Identifies AI generators (OpenAI, Adobe Firefly, Runway etc)
# and real cameras (Sony, Leica, Google Pixel, Samsung S25)
c2pa-python>=0.28.0

# NPR frequency analysis uses existing numpy/scipy — no new deps needed
boto3>=1.34.0
