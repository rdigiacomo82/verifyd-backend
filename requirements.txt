# ============================================================
#  VeriFYD — requirements.txt
# ============================================================

# Web framework
fastapi>=0.100.0
uvicorn>=0.23.0

# Required for Form() and UploadFile in FastAPI
python-multipart>=0.0.6

# Computer vision — cp37-abi3 wheel covers Python 3.7–3.13+
# 4.13.0.92 is the first release with Python 3.13 support
opencv-python-headless==4.13.0.92
numpy>=2.0.0
Pillow>=10.0.0

# HTTP client for direct .mp4 URLs
requests>=2.31.0

# Video downloader — supports TikTok, Instagram, YouTube, and 1000+ platforms
# Install with curl-cffi extra so yt-dlp registers it as impersonation handler
# See: pip install "yt-dlp[default,curl-cffi]" in build.sh
yt-dlp>=2025.1.1

# WebSocket client — used by SMVD RapidAPI render status monitoring
websocket-client>=1.6.0

# OpenAI GPT-4o vision analysis
openai>=1.0.0

# Resend email SDK for OTP verification
resend>=2.0.0

